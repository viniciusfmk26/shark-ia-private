# Sessão 28/04/2026 — Estado do Projeto

> Snapshot da sessão de trabalho. Foco do dia: tema visual Shark Panel + whitelabel + visão global no master.
> Relacionado: [[FIXES]] | [[bugs]] | [[../Empresa/roadmap]]

---

## O que foi feito hoje (28/04/2026)

Sequência cronológica de commits no `/root/Zapflix-Tech` (branch `main`).

### Tema visual / branding (Shark Panel)
- **00:32** `85bec0c2` — feat: tema ciano passada 1 — botões/CTAs verde→cyan (status preservado).
- **03:00** `60d43e10` — feat: tema Shark Panel — fundo azulado `#0a0f1e` coordenado (etapa 4 do tema).
- **04:07** `2b33ebd5` — fix: migrar `color 'emerald' → 'cyan'` em templates (código + banco). Picker deixou de ter opção duplicada.
- **14:44** `758ae437` — fix: trocar ícone coroa por 🦈 no master sidebar (resolve BUG-017 — alinhamento visual master/cliente).
- **14:55** `cd1f1c44` — feat: sidebar topo mais compacto e profissional (`components/layout/sidebar-nav.tsx`, 4±).

### Whitelabel
- **03:28** `b6ff1726` — fix: respeitar `is_active` no sidebar + remover campos inertes (`custom_domain`, `custom_css`) da UI + unificar hook.

### Painel master
- **03:51** `1d0958b2` — feat: visão global de tickets, trials e jobs no master.

---

## Estado do working tree

- `git status` limpo (sem mudanças pendentes / sem untracked).
- Último commit local = último commit no repositório.
- Build/deploy: **não foi executado nesta sessão** após o `cd1f1c44`. Lembrete da AGENTS.md:

  ```bash
  docker build -t zapflix-tech:latest /root/Zapflix-Tech
  docker service update --image zapflix-tech:latest wp_zapflix-web
  ```

  Antes de encerrar, validar se o sidebar compacto (14:55) precisa ir para produção agora ou se entra junto com o próximo lote.

---

## O que está pendente

Recortado do `Empresa/roadmap.md` — itens que **ainda não foram tocados** e seguem em aberto.

### Prioridade 1 — Segurança e estabilidade
- ⏳ **Fix 1.4** — Rotacionar credenciais (PAT GitHub, MinIO, CRON_SECRET).
- ⏳ **Fix 1.5** — Auth em `/api/notifications` (hoje sem proteção).
- ⏳ **Fix 2.1** — IDOR em rotas de analytics.
- ⏳ **Fix 2.2** — `handleSyncContacts` precisa filtrar por `workspace_id`.
- ⏳ **Fix 2.5** — Backups automáticos do Postgres (não há cron de dump).

### Prioridade 2 — Operacional
- ⏳ **Fix 3.1** — Desligar stack Supabase legado (14 containers em `/root/supabase-docker`).
- ⏳ **Fix 3.2** — Desligar PostgREST (`wp-zapflix-postgrest.jomik8.easypanel.host`).
- ⏳ **Fix 3.3** — `lib/short-url.ts` ainda usa `Math.random()`; trocar por `crypto`.
- ⏳ **Fix 3.4** — Redis sem `keyPrefix`; isolar com `zapflix:` (compartilha keyspace com Evolution).
- ⏳ **Fix 3.5** — `zapflix-monitor` (Flask) offline há 3+ semanas; reativar com env vars corretas.
- ⏳ **Fix 3.6** — Agendar crons críticos no supercronic (`plan-expiry`, `trial-followup`, `drip-campaigns`, `pix-followup`, `renewal-check`, `monthly-payroll`, `close-inactive`, `promote-expired-trials`).

### Prioridade 3 — Features
- F-001 Sistema de afiliados — a definir.
- F-002 Ativar crons não agendados — depende de Fix 3.6.
- F-003 Reativar/melhorar `zapflix-monitor` com alertas via WhatsApp — depende de Fix 3.5.
- F-004 Política de retenção de dados (audit_logs com 347 MB sem retenção).

---

## Próxima sessão — sugestão de entrada

1. Validar visualmente o sidebar compacto em produção (deploy do `cd1f1c44`).
2. Atacar **Fix 1.5** (auth em `/api/notifications`) — é pequeno e fecha um buraco do EXCLUDED_PREFIXES.
3. Em paralelo, decidir se **F-001 (afiliados)** entra antes ou depois de zerar Prioridade 1.
